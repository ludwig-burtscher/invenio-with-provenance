401,404c401,407
<     # Note, cannot be done in one line due overloading of boolean
<     # operations permission object.
<     if not permission_factory(record=record).can():
<         from flask_login import current_user
---
>     pm = permission_factory(record=record)
> 
>     from flask_login import current_user
>     import subprocess
>     import json
>     subprocess.Popen(["python3", "/provstore-push.py", json.dumps(vars(current_user)), json.dumps(vars(pm)), json.dumps(vars(record))])
>     if not pm.can():
